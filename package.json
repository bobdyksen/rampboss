{
  "name": "ramp-boss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ramp Boss — arcade airport ground-operations game",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "typescript": "^5.7.3",
    "vite": "^6.2.0",
    "vitest": "^3.0.9"
  }
}
